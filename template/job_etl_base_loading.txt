import sys
import logging
import configparser
from pyspark.sql import *
from pyspark.sql.types import StringType
from pyspark.sql.functions import regexp_replace, col
from etl_framework.utils.logger_utils import *
from etl_framework.utils.spark_utils import *
from etl_framework.utils.module_utility import *

spark = create_iceberg_spark_session(
    s3_warehouse_path=sys.argv[1], app_name=sys.argv[3].split('/')[-1].split('.')[0])

logger = get_logger()


print("="*60)
print("Loading ETL BASE")
print("="*60)

{etl_base_code}